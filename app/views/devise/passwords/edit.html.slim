.bg-light-gray.cover.min-vh-100.ph4.pv3 style="background-image: url(http://farid.co/OS/onboard-bg.jpg)"
  .brand
    a.link.f3.pa3.white-90.hover-white href="/"
      span.fw3 open
      span.fw6 studios

  .center.mw7-ns.mb4.mt6
    = render 'shared/flash_messages'

    h1.cardo.f2.fw3.mb5.tc.white Change your password

    = form_with model: resource, local: true, url: password_path(resource_name), class: 'center ma4 w-80', method: 'put' do |f|
      = f.hidden_field :reset_password_token
      .mb4
        = f.password_field :password, class: "ba #{resource.errors[:password].any? ? 'b--lime' : 'b--white'} bg-white-20 bw1 mb1 montserrat pa3 p-white w-100 white", placeholder: 'New password'
        - resource.errors.full_messages_for(:password).each do |error|
          .mt2.lime = error

      .mb4
        = f.password_field :password_confirmation, class: "ba #{resource.errors[:password_confirmation].any? ? 'b--lime' : 'b--white'} bg-white-20 bw1 mb1 montserrat pa3 p-white w-100 white", placeholder: 'Confirm new password'
        - resource.errors.full_messages_for(:password_confirmation).each do |error|
          .mt2.lime = error

      .mb4.tc
        input.ba.b--white.bg-transparent.f3.fw4.hover-ocean.hover-bg-white.link.montserrat.pa3.pointer.w-100.white type="submit" value="Change my password"

      .flex.justify-between.mb4
        a.f6.hover-white.link.white-90 href=new_user_registration_path Don't have an account?
